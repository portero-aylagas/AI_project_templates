# Limitations

- The starter graph is intentionally small and deterministic.
- Persistence is local JSON by default; production checkpointing should be added
  deliberately.
- Model-driven routing should be tested with malformed and ambiguous outputs.


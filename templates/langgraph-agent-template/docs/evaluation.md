# Evaluation

Evaluate graph workflows by expected state transitions, terminal status,
structured output fields, and recovery behavior for failed nodes.

Use fake model clients for regression tests and live model runs only when
explicitly approved.


# Evaluation

Start with 5 to 20 representative fixture inputs. For each case, record the
expected structured fields, required warnings, and unacceptable output patterns.

Normal evaluation should use fake clients for regression tests. Live evaluation
requires explicit approval because it may use paid services.


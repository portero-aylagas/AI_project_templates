# Limitations

- The starter agent intentionally uses a small fake tool set.
- Agent behavior can be harder to predict than direct calls; add tests for tool
  selection and final answer extraction before changing prompts or tools.
- Do not broaden tool access without updating safety notes and tests.


# Evaluation

Evaluate MCP workflows by allowed tool calls, blocked tool calls, typed tool
results, and failure behavior when a server returns malformed data.

Live server evaluation requires explicit approval.


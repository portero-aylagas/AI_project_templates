# Limitations

- The template does not execute arbitrary tools.
- Tool allowlists must be reviewed before live use.
- MCP server responses are untrusted until validated.
- Logs should include tool names and run IDs, not sensitive payloads.


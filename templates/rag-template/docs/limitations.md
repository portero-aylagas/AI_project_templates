# Limitations

- The starter retriever is simple and deterministic for testing.
- Production vector stores, embedding cache invalidation, and access control
  must be added deliberately.
- Retrieved documents are untrusted input and may contain prompt injection.

